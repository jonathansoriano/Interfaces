Project goes over using Interfaces and when you "implement" an interface to a class, you have to implement that interface how you see fit. 
CAUTION: In BigRectLister.java, there' a small error in the naming convention of the ArrayList called "shortRect". It should''ve been called 
         "bigRect" since if an instance of a rectangle's perimeter is greater than 10, it will be added to the arraylist. 
